#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: Print all possible combination of digits
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	int j = 0;

	do {
		putchar(",");
		putchar(' ');
	} while (j < 9);
	putchar('\n');
	return (o);
}
