#include "main.h"

/**
 * _strlen - function that reutrns the length of a string
 *
 *@s: string to validate
 *
 * Return: Length of a string
 */

int _strlen(char *s)
{
	int t;

	t = 0;

	while (s[t] != '\0')
	{
		t++;
	}
	return (str);
}
